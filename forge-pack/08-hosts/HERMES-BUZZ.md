# Hermes, Buzz and Bossman Wiring

Bossman owns the DashFlo mission persistently. Hermes Kanban owns durable orchestration state. Buzz is the project conversation/status surface. Bossman's business WhatsApp is an urgent approval/blocker surface when its gateway is verified live.

`buzz_guard` stays closed. Do not grant Buzz-driven sessions broad code/terminal authority just to make this project autonomous.

Autonomy is achieved by dispatching implementation to the approved repo-capable coding execution path, not by weakening the control plane.

Nick does not paste wave prompts. Bossman reads the work graph and advances waves automatically when dependencies and predicates are satisfied.

Use `agentos/` in the outer pack as the canonical AgentOS control-plane contract.
