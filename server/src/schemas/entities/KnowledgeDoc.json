{
  "name": "KnowledgeDoc",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Display title of the knowledge item"
    },
    "kind": {
      "type": "string",
      "enum": [
        "doc",
        "note",
        "glossary",
        "template"
      ],
      "default": "doc",
      "description": "doc = reference document, note = free-form note, glossary = term/definition, template = disposition/feedback template"
    },
    "term": {
      "type": "string",
      "description": "For glossary items: the term being defined"
    },
    "content": {
      "type": "string",
      "description": "Body text / definition / template content that DataBot reads as context"
    },
    "active": {
      "type": "boolean",
      "default": true,
      "description": "When true, DataBot includes this item in its context"
    },
    "sort_order": {
      "type": "number",
      "default": 0
    }
  },
  "required": [
    "title"
  ]
}